{
  "name": "reprogate",
  "version": "0.4.0",
  "private": true,
  "description": "研究仓库与金融推理工件的证据优先复现审计平台",
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs --dir . --port 4173",
    "build": "node scripts/build.mjs",
    "start": "node scripts/serve.mjs --dir dist --port 8080",
    "test": "npm run build && node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
